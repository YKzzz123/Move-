前端强制要求：使用 Vue3  <script setup> 语法，使用 Tailwind CSS，坚决不要把 API 密钥硬编码在前端。

后端强制要求：使用 FastAPI 最佳实践，数据库操作必须使用 SQLAlchemy ORM，代码必须包含类型提示 (Type Hints)。

代码格式要求：有清晰的代码注释，不要写冗余代码，保持代码的低耦合性，保持代码的易读性，保持代码的易维护性。

开发流程文档（必须）：完成功能向的代码变更后，同步更新仓库根目录 `项目开发流程.md` 的「开发记录」——新增 dated 小节下的条目，写清影响路径、接口/表结构要点与待办。同一功能反复迭代时只记录重要变更（契约、数据、行为、安全、迁移）；纯样式/文案/微小重构可省略。单次 PR 或连贯任务可合并为一条。